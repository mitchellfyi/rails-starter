Subject: You've been invited to join <%= @workspace.name %>

Hello!

<%= @invited_by.email %> has invited you to join "<%= @workspace.name %>" as a <%= @invitation.workspace_role&.display_name || @invitation.role.humanize %>.

<% if @workspace.description.present? %>
About this workspace:
<%= @workspace.description %>
<% end %>

To accept this invitation, click the link below:
<%= @accept_url %>

This invitation will expire in 7 days. If you don't want to join this workspace, you can safely ignore this email.

Best regards,
The <%= Rails.application.class.parent_name %> Team