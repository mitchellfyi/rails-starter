<% content_for :title, "Welcome - Setup" %>

<div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
      Welcome to Your New Application
    </h2>
    <p class="mt-2 text-center text-sm text-gray-600">
      Let's get you set up in just a few steps
    </p>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-4 shadow sm:rounded-lg sm:px-10">
      <div class="text-center">
        <%= link_to "Get Started", onboarding_step_path(@onboarding_progress.next_step), 
            class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
        
        <div class="mt-4">
          <%= link_to "Skip for now", skip_onboarding_path, 
              method: :post,
              data: { confirm: "Are you sure you want to skip the setup? You can always complete it later from your account settings." },
              class: "text-sm text-gray-600 hover:text-gray-500" %>
        </div>
      </div>
    </div>
  </div>
</div>