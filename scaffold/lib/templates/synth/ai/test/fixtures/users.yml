# Basic user fixtures for testing
# These would typically be provided by the Devise/authentication module

one:
  email: "alice@example.com"
  encrypted_password: "<%= Devise::Encryptor.digest(User, 'password123') %>"

two:
  email: "bob@example.com"
  encrypted_password: "<%= Devise::Encryptor.digest(User, 'password123') %>"