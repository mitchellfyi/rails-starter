<div class="max-w-4xl mx-auto p-6">
  <div class="flex justify-between items-start mb-6">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">My LLM Outputs</h1>
      <p class="text-gray-600 mt-1">View and manage your AI-generated content</p>
    </div>
    <div class="text-sm text-gray-500">
      <%= pluralize(@llm_outputs.count, 'output') %>
    </div>
  </div>

  <div class="space-y-4">
    <% @llm_outputs.each do |output| %>
      <div class="bg-white border border-gray-200 rounded-lg p-6 hover:shadow-md transition-shadow">
        <div class="flex justify-between items-start mb-4">
          <div class="flex-1">
            <h3 class="text-lg font-medium text-gray-900">
              <%= link_to output.template_name, llm_output_path(output), class: "hover:text-blue-600" %>
            </h3>
            <div class="flex items-center space-x-4 mt-2 text-sm text-gray-500">
              <span class="flex items-center">
                <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
                <%= output.model_name %>
              </span>
              <span><%= output.format.upcase %></span>
              <span><%= time_ago_in_words(output.created_at) %> ago</span>
            </div>
          </div>
          
          <div class="flex items-center space-x-2">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                         <%= output.success? ? 'bg-green-100 text-green-800' : 
                             output.failed? ? 'bg-red-100 text-red-800' : 
                             output.processing? ? 'bg-yellow-100 text-yellow-800' : 
                             'bg-gray-100 text-gray-800' %>">
              <%= output.status.humanize %>
            </span>
            
            <% if output.feedback.present? && !output.none? %>
              <span class="text-lg">
                <%= output.thumbs_up? ? '👍' : '👎' %>
              </span>
            <% end %>
          </div>
        </div>

        <% if output.success? %>
          <div class="text-gray-700 text-sm line-clamp-3">
            <%= truncate(output.parsed_output, length: 200) %>
          </div>
        <% elsif output.failed? %>
          <div class="text-red-600 text-sm">
            <%= output.raw_response %>
          </div>
        <% end %>

        <div class="mt-4 flex justify-between items-center">
          <div class="text-xs text-gray-400">
            Job ID: <%= output.job_id %>
          </div>
          
          <div class="flex space-x-2">
            <%= link_to "View", llm_output_path(output), 
                class: "text-blue-600 hover:text-blue-800 text-sm font-medium" %>
            
            <% if output.success? %>
              <%= button_to "Re-run", re_run_llm_output_path(output), 
                  method: :post, 
                  class: "text-green-600 hover:text-green-800 text-sm font-medium bg-transparent border-0 p-0",
                  confirm: "Re-run this job with the same parameters?" %>
              
              <%= button_to "Regenerate", regenerate_llm_output_path(output), 
                  method: :post, 
                  class: "text-orange-600 hover:text-orange-800 text-sm font-medium bg-transparent border-0 p-0",
                  confirm: "Generate new output with the same parameters?" %>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>

  <% if @llm_outputs.empty? %>
    <div class="text-center py-12">
      <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
      </svg>
      <h3 class="mt-2 text-sm font-medium text-gray-900">No LLM outputs</h3>
      <p class="mt-1 text-sm text-gray-500">Get started by creating your first AI job.</p>
    </div>
  <% end %>
</div>